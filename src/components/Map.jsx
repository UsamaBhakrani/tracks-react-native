import { View, StyleSheet, Text } from "react-native";

const Map = () => {
  return (
    <View>
      <Text>I am a Map</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});

export default Map;
